{
  "title": "Tokopedia Product Search Scraper",
  "description": "Scrape product search results from Tokopedia (Indonesia's largest marketplace). Extracts product name, price, discount, shop info, ratings, reviews, and more.",
  "type": "object",
  "schemaVersion": 1,
  "required": ["searchTerms"],
  "properties": {
    "searchTerms": {
      "title": "Search Keywords",
      "description": "Keywords to search for on Tokopedia (e.g., 'laptop gaming', 'sepatu nike'). For bulk scraping, use the 'keywords' field instead.",
      "type": "string",
      "editor": "textfield",
      "prefill": "laptop gaming"
    },
    "keywords": {
      "title": "Bulk Keywords",
      "description": "Scrape multiple keywords in one run. Provide as JSON array: [\"laptop gaming\", \"laptop murah\", \"laptop asus\"]. When provided, 'searchTerms' is ignored.",
      "type": "array",
      "items": { "type": "string" },
      "editor": "stringList",
      "default": []
    },
    "maxPages": {
      "title": "Max Pages",
      "description": "Number of pages to scrape. Each page yields up to ~60 products. Tokopedia typically caps results at ~60 unique products per query, so pages >1 may return duplicates (automatically deduped).",
      "type": "integer",
      "editor": "number",
      "minimum": 1,
      "maximum": 50,
      "default": 1,
      "prefill": 1
    },
    "sortBy": {
      "title": "Sort By",
      "description": "Sort results by a specific field.",
      "type": "string",
      "editor": "select",
      "default": "relevance",
      "enumSuggestedValues": [
        "relevance",
        "newest",
        "price_high",
        "price_low",
        "popular",
        "reviews"
      ],
      "enumTitles": [
        "Relevance (default)",
        "Newest",
        "Highest Price",
        "Lowest Price",
        "Most Popular",
        "Most Reviews"
      ]
    },
    "minPrice": {
      "title": "Minimum Price (IDR)",
      "description": "Filter products with price above this value. Leave 0 for no filter.",
      "type": "integer",
      "editor": "number",
      "minimum": 0,
      "default": 0
    },
    "maxPrice": {
      "title": "Maximum Price (IDR)",
      "description": "Filter products with price below this value. Leave 0 for no filter.",
      "type": "integer",
      "editor": "number",
      "minimum": 0,
      "default": 0
    },
    "minRating": {
      "title": "Minimum Rating",
      "description": "Only return products with this rating or higher. 0 = any rating.",
      "type": "string",
      "editor": "select",
      "default": "0",
      "enumSuggestedValues": [
        "0",
        "1",
        "2",
        "3",
        "4"
      ],
      "enumTitles": [
        "Any rating",
        "1+ star",
        "2+ stars",
        "3+ stars",
        "4+ stars"
      ]
    },
    "officialStore": {
      "title": "Official Store Only",
      "description": "Only return products from official/brand stores.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "location": {
      "title": "Location",
      "description": "Filter by seller city (e.g., 'Jakarta', 'Surabaya'). Leave empty for all.",
      "type": "string",
      "editor": "textfield",
      "default": ""
    }
  }
}
